#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes absolute value of an integer
 *
 * @n: number to check
 *
 * Return: absolute value
 */
int _abs(int x)
{
	int res = abs (x);

	return(res);
}
