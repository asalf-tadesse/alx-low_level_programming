#include "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * main : entry point
 *
 * @program that multiply two numbers
 *
 * return 0
 */
int mul(int a, int b)
{
    int result;
    result = a * b;
    return (result);
}
